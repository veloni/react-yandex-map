/* import { useState } from 'react';

const useOpenSpoiler = () => {
  const [isOpen, setIsOpen] = useState(false);

  const isOpenSpoler = () => {
    setIsOpen(!isOpen);
  }

  return [
    isOpenSpoler,
    isOpen,
  ];
};

export default useOpenSpoiler;  */